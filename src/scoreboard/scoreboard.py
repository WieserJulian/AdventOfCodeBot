class ScoreBoard:

    def __init__(self, id, last_refresh, owner_id, json_content, cookie_value):
        self.id = id
        self.last_refresh = last_refresh
        self.owner_id = owner_id
        self.json_content = json_content
        self.cookie_value = cookie_value
